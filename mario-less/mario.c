// Prompts user for an int size and creates a half-pyramid in the style of Mario

#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string height
    do
    {

    }
    while
    {
        height < 0 || height 
    }
}