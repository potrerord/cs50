// Prints the minimum coins needed to make a given amount of change (in cents)

#include <stdio.h>
#include <cs50.h>

int main(void)
{
    // Subtract 25 cents until < 25 cents remaining, add coins
    // Subtract 10 cents until < 10 cents remaining
    // Subtract 5 cents until < 5 cents remaining
    // Subtract 1 cent until 0 cents remaining
}